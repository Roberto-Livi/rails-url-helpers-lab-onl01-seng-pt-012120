<% if @student.active  %>
<% @student.active = false %>
<% else %>
<% @student.active = true %>
<% end %>

<%= @student.active %>

if @student.active
      "This student is currently inactive."
    else
      "This student is currently active."
    end